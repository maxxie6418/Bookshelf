1. ## 1. 文档目的

   本文件用于指导 AI 编程代理在本仓库中工作。

   本文件只保留高频、稳定、必须遵守的规则。详细产品设计、技术架构、API、数据库、部署、安全和测试说明应放在 `docs/` 目录下的专用文档中。

   AI 编程代理开始任务前，应先阅读本文件，再根据任务类型阅读对应文档和相关代码。

   ## 2. 项目概述

   项目名称：

   - `<项目名称>`
      项目简介：
   - `<用一到三句话说明项目是什么、解决什么问题、面向谁>`
      主要技术栈：
   - 前端：`<前端技术栈>`  
   - 后端：`<后端技术栈>`  
   - 数据库：`<数据库>`  
   - 存储：`<存储方式>`  
   - 部署：`<部署方式>`
   - 项目结构：

   # 3.AI 工作原则

   - 先理解现有实现，再修改代码，保持修改范围最小。
   - 遵循最简原则，优先复用已有组件、函数、服务、类型和配置。
   - 不要引入与任务无关的重构。
   - 不要绕过已有架构边界。
   - 不要基于猜测修改数据库、认证、权限、部署或安全相关代码。
   - 发现需求与现有实现冲突时，应明确指出冲突点。
   - 发现更安全、更简单或更符合现有架构的方案时，应主动说明。
   - 不要在验证失败或未验证时声称任务已完成。

   # 4. 开发原则与架构边界

   - 新增代码应放在职责明确的位置，避免随意新建目录、重复模块或堆积临时逻辑。
   - UI 层只处理展示和交互，复杂逻辑应下沉到业务模块、服务层或专用工具函数中。
   - 页面级组件负责组织结构、状态衔接和数据流转；通用展示、交互控件和业务片段应按职责拆分到合适位置。
   - 组件拆分应保持适度粒度，避免为简单逻辑拆出过多零散组件，也避免将无关逻辑堆砌在单个大型组件中。
   - 当组件变得难以阅读、测试或复用时，应优先与用户建议并确认整理职责边界，而不是继续追加条件分支和临时逻辑。
   - 数据访问、外部 API 调用、配置读取应集中封装，避免散落在页面、组件或路由中。路由层不应堆积复杂业务逻辑。
   - 客户端代码不得读取服务端密钥。
   - 数据模型和迁移应保持稳定，避免因临时需求频繁修改 schema、字段含义或迁移历史；确需调整时，应优先考虑向后兼容、数据迁移路径和旧版本兼容风险。
   - 如确需提交样本数据，应使用小型、脱敏、可公开的示例文件。

   详细架构以 `DOCS/技术架构总指导文件.md` 为准。

   # 5. 禁止或需谨慎的操作

   除非任务明确要求，不要执行以下操作：

   - 大范围重构。
   - 替换技术栈。
   - 新增生产依赖。
   - 擅自改变项目运行环境，包括运行时版本、包管理器、系统依赖、部署方式、端口和环境变量约定。
   - 擅自修改可能导致数据库不兼容或不可逆变化的内容，包括 schema、迁移、数据删除、字段含义和存储路径。

   涉及以下内容时，应先确认影响范围：

   - 数据库结构、迁移和数据兼容性。
   - 认证、权限和安全策略。
   - API 返回结构和错误格式。
   - 部署、环境变量、端口和运行时版本。
   - 文件存储路径、上传目录和对象存储规则。
   - 公共组件、公共类型、公共工具函数和全局配置。

   # 6. 验证与交付格式

   完成任务后，应说明：

   - 修改摘要。
   - 运行的验证命令。
   - 验证结果。
   - 未验证内容或剩余风险。
   - 在META/LOG文件中简述此次修改。

   如果无法运行验证命令，应明确说明原因，不要声称已经验证。

   如果涉及 Git 操作：

   - commit 信息使用中文。
   - 不提交密钥、`.env`、本地数据库、上传文件、测试素材或构建缓存。
   - 提交信息遵循项目约定；如无特殊约定，使用中文 Conventional Commits。
   - Git 操作后，应返回最近提交记录或相关状态信息，便于用户确认。

   # 7.文档说明

   ## 结构

   ```text
   项目根目录/
   ├─ README.md
   ├─ AGENTS.md
   ├─ DOCS/
   │  ├─ 产品需求总指导文件.md
   │  ├─ 产品设计总指导文件.md
   │  ├─ 技术架构总指导文件.md
   │  ├─ API接口手册.md
   │  ├─ 开发命令手册.md
   │  ├─ 数据模型与迁移手册.md
   │  ├─ 文档维护规范.md
   │  ├─ 文档范例.md
   │  └─ 技术决策记录/
   └─ META/
      ├─ SETTINGS/
      ├─ TODO/
      ├─ WORKLIST/
      ├─ LOG/
      ├─ ARCHIVE/
      └─ DATALAB/
   ```

   ## 目录职责

   ### 根目录文档

   - `README.md`：项目介绍、快速开始、安装运行。
   - `AGENTS.md`：AI 编程代理工作规则、文档索引、目录约定、验证要求。

   ### 正式文档

   - `DOCS/`：长期正式文档区。存放需求、设计、架构、API、数据模型、文档规范和技术决策。
   - `DOCS/产品需求总指导文件.md`：产品定位、功能范围、边界和验收方向。
   - `DOCS/产品设计总指导文件.md`：交互原则、信息架构、视觉规则和体验约定。
   - `DOCS/技术架构总指导文件.md`：技术栈、模块边界、数据流和架构约束。
   - `DOCS/API接口手册.md`：接口路径、请求参数、响应结构和错误约定。
   - `DOCS/数据模型与迁移手册.md`：表结构、字段、索引、迁移和兼容规则。
   - `DOCS/文档维护规范.md`：文档状态、版本、修改记录、废弃、归档和命名规则。
   - `DOCS/文档范例.md`：正式文档、时效性文档、日志文档的模板示例。
   - `DOCS/技术决策记录/`：已确认的技术决策、取舍依据和历史原因。

   ### 开发文档

   - `META/`：项目元资料区。存放设定、讨论、计划、日志、归档和测试资料。
   - `META/SETTINGS/`：已确认的流程、功能和模块设计。一般按功能或模块建文件。
   - `META/TODO/`：未完全确定的讨论事项。具有时效性，不作为最终实现依据。
   - `META/WORKLIST/`：已确认待执行或正在执行的计划。具有时效性，不替代长期文档。
   - `META/LOG/`：编码修改日志。按日期记录修改范围、涉及文件、验证情况和剩余风险。
   - `META/ARCHIVE/`：归档资料。存放过期方案、废弃设计、已完成计划、历史版本文档和不再作为当前依据的资料。
   - `META/DATALAB/`：测试素材、样本数据、导入模板、实验文件和临时验证数据。不上传 Git，必须加入 `.gitignore`：。

   ## 维护原则：

   - `DOCS/` 和 `META/` 中的正式文档必须包含文档头部信息和修改记录。
   - 所有文档，头部要注明`文档版本`，`文档状态`，`目的和适用范围`<本文档适用的功能、模块或全局范围>，`权威级别`<全局规则 / 模块规则 / 临时规则 / 历史参考>，`最后更新日期`：`YYYY-MM-DD`和修改记录表格。修改记录表格需要包含`文档版本号`，`应用版本号`，`日期`，`修改摘要`，`修改模型ID`。
   - 文档版本号独立于应用版本号。文档状态使用固定枚举：`草案`、`评审中`、`生效`、`废弃`、`归档`。
   - 长期正式文档放入 `DOCS/`，指导性文档更新以追加为主，不直接覆盖、删除或重写原文。
   - 如需大规模重写，应废弃旧文档，移入 `META/Archive/`，并在原路径创建新文档。
   - 时效性文档文件名应包含月日编号、里程碑号或应用版本号、主题和模型ID。
   - 项目文件应按职责归位，保持结构清晰、命名明确、可追踪。新增文件前，应先确认是否已有合适目录、同类文件或可复用模块。
   - 源码、正式文档、过程资料和测试素材应严格分区。
   - `META/DATALAB/` 默认不纳入版本控制，禁止上传git，应加入 `.gitignore`。

   # 8.任务开始前检查

   AI 编程代理开始修改前，应完成以下检查：

   - 当前任务属于修复、功能、重构、文档、测试、部署还是排错。
   - 是否需要阅读 `DOCS/` 中的对应文档。
   - 是否涉及数据库、认证、权限、部署、安全或 API 返回结构。
   - 是否已有相同功能、相似组件、同类工具函数或既有目录。
   - 是否存在影响范围不明确、需求与现有实现冲突、或需要用户确认的风险点。

   如任务范围明确且风险较低，可以直接执行；如涉及高风险区域，应先说明影响范围和建议方案。

   # 9.工作SOP

   ## 新功能

   1. 了解用户对功能的需求和期望
   2. 查阅需求总纲，以总纲描述为基本原则，用户具体需求为方向
   3. 在模块文档中查询是否有关联，如果没有，且功能足够闭环可以先建立设计文档。
   4. 根据设计文档描述创建功能点
   5. 将功能点转换为技术方案和实现计划。
   6. 依据计划执行
   7. 如果用户在任何一个环节提出的需求与文档已确定的内容冲突，暂停开发立即向用户声明和确认
