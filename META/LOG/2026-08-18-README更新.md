# README 按现有功能更新日志

- **文档版本**：v1.0
- **文档状态**：生效
- **目的和适用范围**：记录根据仓库当前已实现功能更新根目录 `README.md` 的修改，适用于项目说明文档维护
- **权威级别**：临时规则
- **最后更新日期**：2026-08-18

## 修改记录

| 文档版本号 | 应用版本号 | 日期 | 修改摘要 | 修改模型ID |
|-----------|-----------|------|---------|-----------|
| v1.0 | 0.1.0 | 2026-08-18 | README 按现有功能重写：补齐功能亮点、修正技术栈描述、里程碑进度改为实际完成状态、AI 能力如实描述为 AI Agent REST 接口、补数据库 schema 说明 | gstack-lead |

## 修改摘要

背景：原 `README.md` 的「里程碑进度」仍标注 M1–M5 全部「⏳ 未开始」、AI 能力描述为「M4 AI 查询 `/api/query`」，与仓库实际已实现的代码不一致。本次依据 `src/`、`DOCS/`、`META/LOG/` 逐项核对后重写 README，使其如实反映当前功能。

### 变更要点（`README.md`）

- 开头补充项目功能定位（三态 / 分类标签 / 豆瓣抓取 / AI Agent 接口）。
- 新增「功能亮点（当前已实现）」清单：书籍管理（含「记录」字段）、双视图、筛选搜索、分类标签管理、回收站、元数据抓取与封面 R2、AI Agent 接入（Key 鉴权 + 限频 + 禁回收站）、CSV 导入导出、鉴权安全（含首次请求自动建表）、明暗主题等。
- 技术栈章节：封面改为「下载写入 R2 + 站内代理路径下发」、KV 用途补充（Agent Key / 限频 / 抓取节流）、新增可观测性（observability + 错误栈日志）。
- 开发前准备：补充 `.dev.vars` 说明与会话密钥回退提示。
- 数据库迁移章节：说明首次请求自动幂等建表，通常无需手动迁移。
- Secrets 说明：`AI_BASE_URL` / `AI_API_KEY` 标注为「当前预留，不影响核心功能」；`SESSION_SECRET` 标注必填。
- 新增「数据库 schema（D1）」说明。
- 里程碑进度改为已实现状态：M0/M1/M2/M3/M5 ✅，并注明原 M4「AI 查询」实际以 AI Agent REST 接口落地。

## 验证情况

- 纯文档修改，无运行代码变更；本文件为 Markdown，无需 typecheck / build。
- 已人工核对 README 内容与 `src/index.ts` 已挂载路由（health / auth / books / metadata / covers / categories / tags / agent / agent-keys / export / import）、`wrangler.jsonc`、`.dev.vars.example` 及 `META/LOG/2026-08-18-*.md` 各功能日志一致。

## 未验证内容与剩余风险

- README 为说明性文档，功能细节以 `DOCS/` 与源码为准；若后续功能继续迭代，需同步更新本文档。