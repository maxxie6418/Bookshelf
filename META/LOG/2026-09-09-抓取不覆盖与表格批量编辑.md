# 抓取不覆盖与表格批量编辑日志

- **文档版本**：v1.0
- **文档状态**：生效
- **目的和适用范围**：抓取回填策略改为「已有属性不覆盖」，新增表格视图批量行内编辑。适用于书籍表单/详情抽屉抓取回填、列表刷新按钮与书籍列表表格。
- **权威级别**：临时规则
- **最后更新日期**：2026-09-09

## 修改记录

| 文档版本号 | 应用版本号 | 日期 | 修改摘要 | 修改模型ID |
|-----------|-----------|------|---------|-----------|
| v1.0 | 1.2.0 | 2026-09-09 | 抓取元数据回填改为非空不覆盖；表格工具栏新增批量编辑（全部行进入编辑态 + 保存全部/退出） | TRAE |
| v1.1 | 1.2.0 | 2026-09-09 | 批量编辑保存交互修复：脏追踪（仅保存有改动行）、批量态隐藏行内保存/取消按钮、保存按钮加载态与数量汇总提示 | TRAE |

## 修改摘要

- 修改 `src/web/components/book-edit-form.ts`：表单/抽屉「抓取」回填 `fill(meta)` 改为仅回填空字段——输入框已有内容则不覆盖（避免清理用户自定义内容）；想系统自动更新需先清空对应输入框。
- 修改 `src/web/components/book-inline-edit.ts`：
  - `metaToPatch(meta, book)` 增加当前书籍参数，仅当「抓取到值且该书字段为空」才写入 patch。
  - `createInlineEditRow` 返回类型由 `tr` 重构为 `{ row, save, saveSilent, cancel }`，支持单行保存与批量静默保存。
- 修改 `src/web/components/book-list.ts`：
  - 列表「刷新」按钮调用 `metaToPatch(d, b)` 应用不覆盖策略；无空白字段可更新时给出更明确提示。
  - 表格视图工具栏新增「编辑」按钮（非批量态、表格视图、有数据时显示），点击后当前页全部行进入行内编辑态；工具栏切换为「保存全部 / 退出」。
  - 批量态下隐藏视图切换/翻页/添加书籍，避免破坏行集合；排序/状态筛选/收藏/标签筛选时自动退出批量态。
- 修改 `src/web/state.ts`：新增 `batchEdit` 状态标志。
- v1.1 追加：修改 `src/web/components/book-inline-edit.ts` 与 `book-list.ts`：
  - 行内编辑行新增脏追踪（`isDirty`/`markClean`），状态/分类/评分/收藏/标签任一改动即标记。
  - 批量模式（`batch: true`）下行内「保存/取消」按钮隐藏，改动统一由工具栏「保存全部」提交，「退出」放弃；避免误点单行保存/取消破坏批量态。
  - 「保存全部」仅对改动行发起 PATCH（未改动行跳过，不再逐行无差别写库）；按钮进入「保存中…」加载态防重复提交；完成后 toast 汇总「已保存 N 本，M 本无改动」，无改动时提示「没有修改的内容」。
- 未修改数据库 schema、认证、API 返回结构。

## 验证情况

- `npm run typecheck`：通过。
- `npm run build`：Vite 构建成功（约 3.4s）；命令末尾因 TRAE Sandbox 禁止 Wrangler 写入用户目录日志而返回非零退出码，非代码问题。
- 本地 `wrangler dev` + 浏览器（agent-browser）端到端验证：
  - 批量态下工具栏仅剩「保存全部/退出」，每行不再出现行内保存/取消按钮，仅保留收藏勾选。
  - 不改动直接「保存全部」：立即退出批量态并提示无修改（不发请求）。
  - 仅改动《百年孤独》评分为 9.5 后「保存全部」：快速只保存该书（数据库 rating=9.5），其余未改动书未被写库，退出批量态。
  - 单行保存路径（非批量行内编辑）回归正常。
- 项目未提供独立 lint 脚本。

## 剩余风险

- 编辑弹窗/抽屉的「抓取回填跳过已填」逻辑与列表刷新共用同一语义，但未在真实豆瓣抓取场景下逐表单核对（与列表刷新共用 `fill` 空判断，代码路径一致，typecheck 通过）。
- 批量编辑仅提供当前页（PAGE_SIZE=60）范围内；跨页需分页后分别批量处理。
- 未在生产环境回归，建议部署后抽查。
