# 收藏按钮闪烁修复日志

- **文档版本**：v1.0
- **文档状态**：生效
- **目的和适用范围**：记录书籍列表收藏按钮交互与局部更新优化，适用于前端书籍列表组件。
- **权威级别**：临时规则
- **最后更新日期**：2026-09-08

## 修改记录

| 文档版本号 | 应用版本号 | 日期 | 修改摘要 | 修改模型ID |
|-----------|-----------|------|---------|-----------|
| v1.0 | 1.2.0 | 2026-09-08 | 收藏成功后采用按钮与当前书籍局部更新，避免普通列表触发全量刷新 | TRAE |

## 修改摘要

- 修改 `src/web/components/book-list.ts`。
- 收藏请求成功后直接更新当前收藏按钮状态和当前书籍对象。
- 普通列表收藏不再调用全量 `refresh`，避免列表及侧栏连续重建导致页面闪烁。
- “只看收藏”模式下取消收藏仍刷新当前列表，以移除不符合筛选条件的书籍。
- 未修改数据库、API、认证及全局样式。

## 验证情况

- `npm run typecheck`：通过。
- `npm run build`：Vite 构建代码阶段通过，但命令末尾因 TRAE Sandbox 禁止 Wrangler 写入用户目录日志而返回失败。
- `git diff --check`：通过。
- 项目未提供独立 lint 脚本。

## 剩余风险

- 尚未在浏览器中完成实际点击回归，建议分别检查表格视图、网格视图和“只看收藏”模式。
