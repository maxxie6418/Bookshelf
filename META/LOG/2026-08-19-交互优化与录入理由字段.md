# 前端交互优化与新增「录入理由」字段日志

- **文档版本**：v1.11
- **文档状态**：生效
- **目的和适用范围**：记录侧栏交互重构、AI Key 可复制、详情抽屉调整与新增「录入理由」字段的修改，适用于前端组件、后端书籍/AI Key 接口及数据模型
- **权威级别**：临时规则
- **最后更新日期**：2026-08-19

## 修改记录

| 文档版本号 | 应用版本号 | 日期 | 修改摘要 | 修改模型ID |
|-----------|-----------|------|---------|-----------|
| v1.0 | 0.1.0 | 2026-08-19 | 状态改为顶栏按钮组；侧栏分类/标签分区分割线与内联管理；AI Key 加密存储+reveal 按需复制；详情抽屉记录→笔记、新增录入理由、移入回收站贴左下；底部四按钮改方形按钮组；网格卡片顶部圆角 | gstack-lead |
| v1.1 | 0.1.0 | 2026-08-19 | 调整：标签区固定在侧栏中部（分类区独立滚动、标签区固定不随滚动）；底部四按钮由 2×2 田字格改为一排横向按钮组 | gstack-lead |
| v1.2 | 0.1.0 | 2026-08-19 | 布局修正：详情抽屉底部操作栏改为固定贴底（独立于滚动主体）；侧栏分类区固定占上半部 50%、标签区从中部延伸至底部；底部四按钮缩小为紧凑横排 | gstack-lead |
| v1.3 | 0.1.0 | 2026-08-19 | 样式互换与精简：书签页改为盒式卡片、设置/退出改为平铺（位置不变只换样式）；移除网格卡片底部渐变线条；表格视图上下留白收紧、更紧凑 | gstack-lead |
| v1.4 | 0.1.0 | 2026-08-19 | 详情抽屉侧滑占比加大（480/520→600/680）；README 新增 pic 截图展示；AI Agent 接入页预置「系统提示词」与「能力说明」两段可一键复制内容 | gstack-lead |
| v1.5 | 0.1.0 | 2026-08-19 | 精简：删除 AI 接入页「能力说明」提示词（与系统提示词冲突，仅保留系统提示词一段）；全局字体由手写风 LXGW WenKai 改为系统无衬线字体 | gstack-lead |
| v1.6 | 0.1.0 | 2026-08-19 | 详情抽屉展示重构：书籍属性块统一容器框起来；自行填写区与属性块加分割线；笔记/录入理由支持内联小按钮直接编辑保存（不进全表单） | gstack-lead |
| v1.7 | 0.1.0 | 2026-08-19 | 修复：笔记/录入理由内联编辑在「取消/保存」后内容消失——`renderView` 未移除 `viewEl` 的 `hidden` 类，改为进入视图态时显式复位可见性 | gstack-lead |
| v1.8 | 0.1.0 | 2026-08-19 | 详情抽屉头部封面改为大幅模糊渐变填充背景并压缩高度（正文上移）；简介并入「书籍属性」容器、超 6 行滚动浏览 | gstack-lead |
| v1.9 | 0.1.0 | 2026-08-19 | AI 接入提示词重排：接口与 Key 配置放末尾便于先复制提示词再粘 Key；Base URL 动态取自 `location.origin` 并提供独立复制框。详情抽屉：无封面时兜底占位封面；简介区域始终预留（空时显示「暂无简介」）避免布局上下跳动 | gstack-lead |
| v1.10 | 0.1.0 | 2026-08-19 | 列表分页：书籍/回收站列表按页加载（每页 60，limit/offset 拉取），超过不再硬截断；翻页控件含上一页/下一页与页码指示；筛选/排序变化自动回第 1 页。AI 接入页 Base URL 复制框移至提示词复制之下、Token 复制之上 | gstack-lead |
| v1.11 | 0.1.0 | 2026-08-19 | AI Agent 接入弹窗布局优化：弹窗加宽至 `max-w-3xl`（modal 支持自定义宽度）；内容改为「Step 1 提示词 / Step 2 接口地址 / Step 3 Key」三步分段，各卡片内边距加大、块间间距拉大，缓解紧凑感 | gstack-lead |

## 修改摘要

按用户四点需求与三项补充细节实现，共改动 22 个源码文件 + 新增 1 个迁移。

### 需求 1 — 状态改为顶部按钮组
- `src/web/components/book-list.ts`：移除右侧工具栏的「状态」下拉 `<select>`；在顶栏标题「全部书籍」旁新增「全部 / 未读 / 在读 / 读完」分段按钮组，当前项以强调色高亮。

### 需求 2 — 侧栏分类 / 标签分区 + 分割线
- `src/web/components/app-shell.ts renderSidebar`：分类区占上半部、标签区延伸至底部快捷图标前，两区之间增加细分隔线；各区独立滚动与间距。

### 需求 3 — AI Key 可复制（加密存储 + reveal 按需取）
- `src/lib/agent-key.ts`：用 SESSION_SECRET 派生 SHA-256 密钥对明文做 AES-GCM 加密，仅存密文+IV+前4/后4位（不存明文）；新增 `revealAgentKey`。
- `src/lib/session.ts`：导出 `getSessionSecret` 供复用同一有效 secret。
- `src/api/agent-keys.ts`：创建时传入 secret；新增 `POST /api/agent-keys/:hash/reveal` 按需回显明文。
- `src/web/api.ts`：新增 `revealAgentKey` 客户端方法。
- `src/web/components/settings-panel.ts`：Key 列表显示「前4+星号+后4」掩码；新增「复制」按钮点击时调用 reveal 复制明文；老 Key（无密文）安全降级（无后4/不可复制）。

### 需求 4 — 分类 / 标签侧栏内联管理（不弹窗，避免 UI 推挤）
- `src/web/components/manage-taxonomy.ts`：由弹窗改为 `renderTaxonomyManage` 内联渲染进侧栏分区；已存在项带「改名 / 删除」，末尾默认「待命名」新增行（命名后回车/保存创建，为空忽略即撤回）；行高固定、就地置换避免推挤。
- `src/web/state.ts`：新增 `taxEdit` 编辑态字段。
- `src/web/components/app-shell.ts`：分类/标签头部「管理 / 完成」切换内联编辑态。

### 需求 5 — 网格卡片顶部圆角
- `src/web/components/book-list.ts renderGrid`：封面容器补 `rounded-t-xl overflow-hidden`，使卡片四角圆角一致。

### 需求 6 — 详情抽屉
- `src/web/components/detail-drawer.ts`：展示态「记录」标签改「笔记」，新增「录入理由」展示块；底部改为「移入回收站」贴左下角、右排「关闭 / 编辑」；编辑态同步「笔记 / 录入理由」字段。
- 「录入理由」字段（≤1000 字）全链路：`src/lib/schema.ts`（建表列）、`src/lib/bootstrap.ts`（幂等补列）、`migrations/0002_add_reason.sql`（新增）、`src/lib/books.ts`（BookInput/Row/Create/Update）、`src/api/books.ts` + `src/api/agent.ts`（zod 校验）、`src/lib/csv.ts` + `src/api/export.ts` + `src/api/import.ts`（CSV 导入导出列）、`src/web/types.ts`、`src/web/components/book-edit-form.ts`、`src/web/components/book-form.ts`、`src/web/components/detail-drawer.ts`。

### 需求 7 — 底部四按钮改组合方形按钮组
- `src/web/components/app-shell.ts`：主题 / Agent / 设置 / 退出 由独立图标按钮改为 2×2 组合方形按钮组，与上方快捷链接样式区分。

### v1.3 — 底部样式互换与精简
- `src/web/components/app-shell.ts`：书签页（GitHub / Cloudflare / 豆瓣）改为盒式小方块卡片样式；设置/退出四按钮改为平铺扁平样式，二者位置不变仅互换样式。
- `src/web/components/book-list.ts renderGrid`：移除网格卡片底部的渐变细线条（`after:*` 渐变类）。
- `src/web/components/book-list.ts renderTable`：表头单元格 `py-3`→`py-2`、数据单元格 `py-4`→`py-2`，使表格上下留白收紧、整体更紧凑。

### v1.4 — 抽屉加宽 / README 截图 / AI 预置提示词
- `src/web/components/detail-drawer.ts`：详情抽屉宽度由 `sm:480 / lg:520` 加大为 `sm:600 / lg:680`，侧滑占比更多、内容更舒展。
- `README.md`：新增「界面预览」小节，以 2×2 表格引用 `pic/` 下四张截图（书架页 / 书籍属性 / 书籍编辑 / AI 接入）。
- `src/web/components/settings-panel.ts`：AI Agent 接入面板新增 `copyablePrompt` 组件与两段预置内容（`PROMPT_SETUP` 系统提示词接入说明、`PROMPT_CAPS` 能力说明），各带「复制」按钮，可一键复制整段发给外部 AI。

### v1.5 — 精简 AI 提示词 / 更换全局字体
- `src/web/components/settings-panel.ts`：删除「能力说明」提示词（`PROMPT_CAPS`）及其展示块——与系统提示词内容冲突，仅保留「系统提示词（接入说明）」一段，供一键复制发给外部 AI。
- `src/web/style.css`、`tailwind.config.js`：全局正文/`sans` 字体由手写风格 `LXGW WenKai` 改为系统无衬线字体栈（`-apple-system / PingFang SC / Microsoft YaHei / Noto Sans SC` 等），标题区保留 `Noto Serif SC` 衬线，去除手写观感。

### v1.6 — 详情抽屉展示重构
- `src/web/components/detail-drawer.ts` `displayBody`：
  - 书籍本身属性（作者/译者/出版社/出版年/ISBN/页数/豆瓣链接）统一放入带边框的「书籍属性」容器块。
  - 简介、标签之后，与下方「我的记录」区之间增加分割线。
- `src/web/components/detail-drawer.ts` 新增 `editableMemoBlock`：笔记（≤2000 字）与录入理由（≤1000 字）支持头部小「编辑」按钮内联展开 textarea、字数统计与「保存/取消」，直连 `api.updateBook` 保存，无需进入全表单编辑；保存后刷新列表与侧栏。

### v1.7 — 修复内联编辑后内容消失
- `src/web/components/detail-drawer.ts` `editableMemoBlock.renderView`：进入视图态时显式 `viewEl.classList.remove('hidden')` 与 `editEl.classList.add('hidden')`。此前「取消/保存」只隐藏编辑块、未解除视图块隐藏，导致内容从属性栏消失。

### v1.8 — 头部封面填充化 / 简介并入属性容器
- `src/web/components/detail-drawer.ts` `coverHeader`：不再居中完整展示封面；改为封面放大 + 模糊 + 大留白做渐变填充背景，叠加 surface 渐变遮罩保证标题可读；头部高度由 `h-56/64` 压缩为 `h-40/48`，正文整体上移。
- `src/web/components/detail-drawer.ts` `displayBody`：简介（description）并入「书籍属性」容器内（分割线分隔），设置 `max-h-36 overflow-y-auto`，超过约 6 行时滚动浏览。
- 移除不再使用的 `coverBlock` 及 `renderCoverPlaceholder` 引入。

### v1.9 — AI 提示词重排 / 详情抽屉兜底与预留
- `src/web/components/settings-panel.ts`：`PROMPT_SETUP` 常量改为 `promptSetupBody()` 动态生成——Base URL 取 `window.location.origin` 自动填入；接口与 Key 配置说明移至提示词末尾，便于用户先复制提示词、再复制生成的 Agent Key 粘入鉴权位置；新增 `copyableValue` 独立显示并一键复制 Base URL。
- `src/web/components/detail-drawer.ts` `coverHeader`：无封面时用 `renderCoverPlaceholder` 兜底占位封面，避免头部空白。
- `src/web/components/detail-drawer.ts` `displayBody`：简介区域无论是否有内容都预留（空时显示「暂无简介」），避免内容上下跳动。

### v1.10 — 列表分页 / 提示词区 Base URL 位置微调
- `src/web/state.ts`：新增 `page` 状态（1 起）。
- `src/web/refresh.ts`：导出 `PAGE_SIZE = 60`；`refresh(resetPage = true)` 主查询带 `limit/offset` 按页拉取；默认（筛选/排序/保存等变化）自动回第 1 页，翻页时传 `false` 保持目标页；非回收站视图始终并行拉取未筛选全集用于侧栏统计，避免分页后侧栏计数失真。
- `src/web/components/book-list.ts`：列表底部新增翻页控件（上一页/下一页 + 「共 N 本 · 第 X/Y 页」），首页禁上一页、末页禁下一页；网格与表格视图均生效。
- `src/web/components/settings-panel.ts`：AI 接入页「接口 Base URL」复制框调整到「系统提示词」复制之下、Token 复制之上，并同步更新引导文案顺序。

### v1.11 — AI Agent 接入弹窗布局优化
- `src/web/ui.ts` `modal`：新增可选 `sizeClass` 参数（默认 `max-w-2xl`，向后兼容）。
- `src/web/components/settings-panel.ts`：`openAgentSettings` 弹窗加宽至 `max-w-3xl`；内容改为「Step 1 复制系统提示词 / Step 2 复制接口地址 / Step 3 生成并复制 Agent Key」三步分段；`copyablePrompt`、`copyableValue`、Key 列表卡片内边距加大，块间间距 `mt-5/mt-6` 拉大；底部「能力与限制」用顶部分割线独立成段，缓解整体紧凑感。

## 验证情况

- `npm run typecheck`：通过（worker + web 双工程）。
- `npm run build`：通过（18 模块，产物正常）。
- `prepare-kv` 未登录 Cloudflare 跳过，`wrangler.jsonc` 未被注入 KV ID（未改动）。

## 未验证内容与剩余风险

- 变更 SESSION_SECRET 后，历史 AI Key 无法 reveal 回显（bearer 鉴权不受影响），属预期的安全行为。
- 「录入理由」为新增 DB 列，需在既有部署上先执行一次迁移或依赖运行时自动补列（首次请求 ensureBookReasonColumn）；全新部署走 schema/migration。
- 内联分类/标签管理为交互重构，需在真实浏览器中人工确认无布局推挤与焦点问题（typecheck/build 已通过，未做浏览器端到端验证）。