# 数据模型与迁移手册

- **文档版本**：v1.1
- **文档状态**：草案
- **目的和适用范围**：书单管理工具（Bookshelf）的数据库 Schema、字段语义、索引、约束、枚举与迁移策略。适用于 D1（SQLite）建表、迁移演进与数据兼容评审。
- **权威级别**：模块规则
- **最后更新日期**：2026-08-17

## 修改记录

| 文档版本号 | 应用版本号 | 日期         | 修改摘要                                                                                                                                                                                                          | 修改模型ID      |
| ----- | ----- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| v1.0  | -     | 2026-08-17 | 初版。users/books/categories/tags/book_tags/settings 全量 DDL、枚举、初始迁移、迁移策略                                                                                                                                         | gstack-lead |
| v1.1  | -     | 2026-08-17 | 评审修订：books 增 translator/publish_year/page_count/original_title/started_at/finished_at/deleted_at；封面改 Image Resizing 代理（不写 R2）；AI_API_KEY/SESSION_SECRET 走 secret；会话签名 Cookie、KV 仅封锁；初始用户由 setup seed；软删+回收站设计 | gstack-lead |

---

## 1. 概述

- **引擎**：Cloudflare D1（SQLite）。单租户、单用户，书籍 ≤ 1000 本。
- **时间字段**：统一存 UTC 文本 `YYYY-MM-DD HH:MM:SS`（SQLite 无原生时间类型，使用 `datetime('now')` 默认值时为 UTC）。
- **命名**：snake_case；表/列全小写；布尔用 `INTEGER` 的 `0/1`。
- **字符集**：SQLite 默认 UTF-8，无需额外声明。
- **删除策略（软删除 + 回收站）**：books 增加 `deleted_at`；正常查询 `WHERE deleted_at IS NULL`，回收站 `WHERE deleted_at IS NOT NULL`；二次确认后物理删除，`book_tags` 因 `ON DELETE CASCADE` 级联清除。详见 §3.2 / §5。

## 2. 枚举与编码

| 字段                         | 取值                                                            | 说明                        |
| -------------------------- | ------------------------------------------------------------- | ------------------------- |
| books.status               | `unread` / `reading` / `finished`                             | 未读 / 在读 / 读完（3 态，由 5 态简化） |
| books.source               | `douban` / `neodb` / `openlibrary` / `googlebooks` / `manual` | 元数据来源，用于追溯与抓取兜底           |
| users.is_admin             | `0` / `1`                                                     | 单用户恒为 1                   |
| users.must_change_password | `0` / `1`                                                     | 首次部署强制改口令                 |
| settings.key               | 自由字符串（如 `ai_base_url`）                                        | 密钥类不入库，走 secret           |

## 3. 表结构详述

### 3.1 users（账户，单行）

| 字段                   | 类型      | 约束                                 | 说明              |
| -------------------- | ------- | ---------------------------------- | --------------- |
| id                   | INTEGER | PK AUTOINCREMENT                   |                 |
| username             | TEXT    | NULL                               | 展示用             |
| display_name         | TEXT    | NULL                               |                 |
| password_hash        | TEXT    | NOT NULL                           | bcrypt / argon2 |
| is_admin             | INTEGER | NOT NULL DEFAULT 1                 |                 |
| must_change_password | INTEGER | NOT NULL DEFAULT 1                 | 首登强改            |
| created_at           | TEXT    | NOT NULL DEFAULT (datetime('now')) |                 |
| updated_at           | TEXT    | NOT NULL DEFAULT (datetime('now')) |                 |

> **初始用户**：由 `scripts/setup` 在首次部署时 seed 一行 admin（`must_change_password=1`），初始口令取自 `INITIAL_ADMIN_PASSWORD` secret，首登强制改口令。

### 3.2 books（核心实体）

| 字段             | 类型      | 约束                                                                                         | 说明                                                 |
| -------------- | ------- | ------------------------------------------------------------------------------------------ | -------------------------------------------------- |
| id             | INTEGER | PK AUTOINCREMENT                                                                           |                                                    |
| title          | TEXT    | NOT NULL                                                                                   | 书名                                                 |
| author         | TEXT    | NULL                                                                                       | 作者（多作者用 `/` 分隔）                                    |
| translator     | TEXT    | NULL                                                                                       | 译者                                                 |
| publisher      | TEXT    | NULL                                                                                       | 出版社                                                |
| publish_year   | INTEGER | NULL                                                                                       | 出版年                                                |
| page_count     | INTEGER | NULL                                                                                       | 页数                                                 |
| original_title | TEXT    | NULL                                                                                       | 原作名                                                |
| isbn           | TEXT    | NULL                                                                                       | ISBN-13，导入去非数字字符（ISBN-10 归一化为 13 位用于匹配）            |
| description    | TEXT    | NULL                                                                                       | 简介（已去除 HTML）                                       |
| cover_url      | TEXT    | NULL                                                                                       | 封面远程 URL（经 Cloudflare Image Resizing 按需代理，首版不写 R2） |
| douban_url     | TEXT    | NULL                                                                                       | 来源链接                                               |
| rating         | REAL    | NULL                                                                                       | 评分                                                 |
| status         | TEXT    | NOT NULL DEFAULT 'unread' CHECK IN ('unread','reading','finished')                         | 3 态                                                |
| category_id    | INTEGER | REFERENCES categories(id) ON DELETE SET NULL                                               |                                                    |
| sort_order     | INTEGER | NOT NULL DEFAULT 0                                                                         | 手动排序（P2）                                           |
| source         | TEXT    | NOT NULL DEFAULT 'manual' CHECK IN ('douban','neodb','openlibrary','googlebooks','manual') | 来源                                                 |
| started_at     | TEXT    | NULL                                                                                       | 开始阅读时间（状态变"在读"时写入，供 AI 时间筛选）                       |
| finished_at    | TEXT    | NULL                                                                                       | 读完时间（状态变"读完"时写入）                                   |
| deleted_at     | TEXT    | NULL                                                                                       | 软删标记；NULL=在库，非 NULL=回收站                            |
| created_at     | TEXT    | NOT NULL DEFAULT (datetime('now'))                                                         |                                                    |
| updated_at     | TEXT    | NOT NULL DEFAULT (datetime('now'))                                                         |                                                    |

索引：`idx_books_status(status)`、`idx_books_category(category_id)`、`idx_books_updated(updated_at)`、`idx_books_isbn(isbn)`、`idx_books_deleted(deleted_at)`。

### 3.3 categories（自定义分类）

| 字段    | 类型      | 约束                         | 说明         |
| ----- | ------- | -------------------------- | ---------- |
| id    | INTEGER | PK AUTOINCREMENT           |            |
| name  | TEXT    | NOT NULL                   | 分类名        |
| color | TEXT    | NOT NULL DEFAULT '#8a8274' | 颜色标识（十六进制） |

### 3.4 tags（标签）

| 字段   | 类型      | 约束               | 说明  |
| ---- | ------- | ---------------- | --- |
| id   | INTEGER | PK AUTOINCREMENT |     |
| name | TEXT    | NOT NULL UNIQUE  | 标签名 |

### 3.5 book_tags（多对多）

| 字段      | 类型                | 约束                                     | 说明 |
| ------- | ----------------- | -------------------------------------- | -- |
| book_id | INTEGER           | REFERENCES books(id) ON DELETE CASCADE |    |
| tag_id  | INTEGER           | REFERENCES tags(id) ON DELETE CASCADE  |    |
| PK      | (book_id, tag_id) |                                        |    |

### 3.6 settings（键值设置）

| 字段         | 类型   | 约束                                 | 说明                                                                                |
| ---------- | ---- | ---------------------------------- | --------------------------------------------------------------------------------- |
| key        | TEXT | PK                                 | 如 `ai_base_url`                                                                   |
| value      | TEXT | NULL                               | 非密钥配置（如 `ai_base_url`）；`AI_API_KEY` / `SESSION_SECRET` 等密钥走 `wrangler secret`，不入库 |
| updated_at | TEXT | NOT NULL DEFAULT (datetime('now')) |                                                                                   |

### 3.7 ai_query_log（可选，M4）

| 字段          | 类型      | 约束                                 | 说明          |
| ----------- | ------- | ---------------------------------- | ----------- |
| id          | INTEGER | PK AUTOINCREMENT                   |             |
| query_text  | TEXT    | NULL                               | 用户自然语言      |
| filter_json | TEXT    | NULL                               | LLM 返回的过滤条件 |
| row_count   | INTEGER | NULL                               | 命中数         |
| ip          | TEXT    | NULL                               |             |
| ok          | INTEGER | NOT NULL DEFAULT 1                 |             |
| created_at  | TEXT    | NOT NULL DEFAULT (datetime('now')) |             |

## 4. 初始迁移 `migrations/0000_init.sql`

```sql
-- 账户
CREATE TABLE users (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  username TEXT,
  display_name TEXT,
  password_hash TEXT NOT NULL,
  is_admin INTEGER NOT NULL DEFAULT 1,
  must_change_password INTEGER NOT NULL DEFAULT 1,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now'))
);

-- 书籍
CREATE TABLE books (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  title TEXT NOT NULL,
  author TEXT,
  translator TEXT,
  publisher TEXT,
  publish_year INTEGER,
  page_count INTEGER,
  original_title TEXT,
  isbn TEXT,
  description TEXT,
  cover_url TEXT,
  douban_url TEXT,
  rating REAL,
  status TEXT NOT NULL DEFAULT 'unread'
    CHECK (status IN ('unread','reading','finished')),
  category_id INTEGER REFERENCES categories(id) ON DELETE SET NULL,
  sort_order INTEGER NOT NULL DEFAULT 0,
  source TEXT NOT NULL DEFAULT 'manual'
    CHECK (source IN ('douban','neodb','openlibrary','googlebooks','manual')),
  started_at TEXT,
  finished_at TEXT,
  deleted_at TEXT,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now'))
);
CREATE INDEX idx_books_status ON books(status);
CREATE INDEX idx_books_category ON books(category_id);
CREATE INDEX idx_books_updated ON books(updated_at);
CREATE INDEX idx_books_isbn ON books(isbn);
CREATE INDEX idx_books_deleted ON books(deleted_at);

-- 分类
CREATE TABLE categories (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,
  color TEXT NOT NULL DEFAULT '#8a8274'
);

-- 标签
CREATE TABLE tags (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL UNIQUE
);

-- 书籍-标签多对多
CREATE TABLE book_tags (
  book_id INTEGER NOT NULL REFERENCES books(id) ON DELETE CASCADE,
  tag_id INTEGER NOT NULL REFERENCES tags(id) ON DELETE CASCADE,
  PRIMARY KEY (book_id, tag_id)
);

-- 设置
CREATE TABLE settings (
  key TEXT PRIMARY KEY,
  value TEXT,
  updated_at TEXT NOT NULL DEFAULT (datetime('now'))
);

-- AI 查询日志（可选）
CREATE TABLE ai_query_log (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  query_text TEXT,
  filter_json TEXT,
  row_count INTEGER,
  ip TEXT,
  ok INTEGER NOT NULL DEFAULT 1,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);
```

## 5. 迁移策略

- **执行方式**：`wrangler d1 execute bookshelf --file=./migrations/0000_init.sql`（由 `scripts/setup` 调用）。
- **增量演进**：后续变更新建 `0001_*.sql` 等，只做 `ADD COLUMN` / `CREATE INDEX` 等向后兼容操作；避免 `DROP COLUMN` 或破坏性改字段含义。
- **兼容规则**：新增列必须带 `DEFAULT` 或允许 `NULL`；枚举扩展时先放宽 `CHECK` 再回填；`status` 三态为终态，不再回退到 5 态。
- **本地**：`wrangler dev` 使用本地 D1 文件，迁移脚本同套。
- **软删除与回收站**：books 用 `deleted_at` 标记删除，正常查询 `WHERE deleted_at IS NULL`；回收站 `WHERE deleted_at IS NOT NULL`；二次确认后 `DELETE` 物理删除，`book_tags` 因 `ON DELETE CASCADE` 级联清除。新增列均带 `DEFAULT NULL`，向后兼容。
- **数据量**：≤1000 本，单表全量查询可接受，无需分表/分区。

## 6. 数据量与索引建议

- books 全表扫描 <1000 行，索引主要用于筛选分支（status / category_id / isbn）与排序（updated_at）。
- book_tags 随书籍增长线性，联合主键即聚簇，查询走 book_id / tag_id 均快。
- 不做全文索引；中文搜索用 `LIKE` 前缀/包含匹配（量级小，可接受）。

